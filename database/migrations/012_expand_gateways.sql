ALTER TABLE `gateways`
  ADD COLUMN IF NOT EXISTS `description` TEXT DEFAULT NULL AFTER `display_name`,
  ADD COLUMN IF NOT EXISTS `logo_url` VARCHAR(500) DEFAULT '' AFTER `description`,
  ADD COLUMN IF NOT EXISTS `merchant_id` VARCHAR(255) DEFAULT '' AFTER `logo_url`,
  ADD COLUMN IF NOT EXISTS `supported_currencies` VARCHAR(255) DEFAULT 'USD' AFTER `merchant_id`,
  ADD COLUMN IF NOT EXISTS `min_amount` DECIMAL(10,2) DEFAULT 0.00 AFTER `supported_currencies`,
  ADD COLUMN IF NOT EXISTS `max_amount` DECIMAL(10,2) DEFAULT 0.00 AFTER `min_amount`,
  ADD COLUMN IF NOT EXISTS `processing_fee` DECIMAL(10,2) DEFAULT 0.00 AFTER `max_amount`,
  ADD COLUMN IF NOT EXISTS `fee_type` VARCHAR(20) DEFAULT 'fixed' AFTER `processing_fee`,
  ADD COLUMN IF NOT EXISTS `webhook_url` VARCHAR(500) DEFAULT '' AFTER `fee_type`,
  ADD COLUMN IF NOT EXISTS `webhook_secret` VARCHAR(255) DEFAULT '' AFTER `webhook_url`,
  ADD COLUMN IF NOT EXISTS `success_url` VARCHAR(500) DEFAULT '' AFTER `webhook_secret`,
  ADD COLUMN IF NOT EXISTS `cancel_url` VARCHAR(500) DEFAULT '' AFTER `success_url`,
  ADD COLUMN IF NOT EXISTS `is_default` TINYINT(1) DEFAULT 0 AFTER `cancel_url`,
  ADD COLUMN IF NOT EXISTS `sandbox_client_id` VARCHAR(255) DEFAULT '' AFTER `is_default`,
  ADD COLUMN IF NOT EXISTS `sandbox_secret` VARCHAR(255) DEFAULT '' AFTER `sandbox_client_id`,
  ADD COLUMN IF NOT EXISTS `live_client_id` VARCHAR(255) DEFAULT '' AFTER `sandbox_secret`,
  ADD COLUMN IF NOT EXISTS `live_secret` VARCHAR(255) DEFAULT '' AFTER `live_client_id`,
  ADD COLUMN IF NOT EXISTS `brand_name` VARCHAR(255) DEFAULT '' AFTER `live_secret`,
  ADD COLUMN IF NOT EXISTS `invoice_prefix` VARCHAR(50) DEFAULT '' AFTER `brand_name`;
